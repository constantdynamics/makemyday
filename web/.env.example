# Make My Day — web env
# These are SAFE to expose in client code; they are protected by Supabase RLS.
# If unset, the app falls back to the baked-in project defaults (see src/lib/config.ts),
# so the GitHub Pages build works without any CI secrets.
VITE_SUPABASE_URL=https://wmdopfocqufsquzvemka.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_0vzeEC0FttISlsEiDaFCnw_N7bjjNym
